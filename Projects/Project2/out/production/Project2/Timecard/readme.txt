//1. Need to check accessing json as string, editing string dept no and the
// creating dept obj
//2. Check if department update requires a set variable before calling
update department
//3. Correct delete department. need to delete employees first
